H, M = map(int, input().split())  # 시간(H)과 분(M)을 입력받음

R = (60 - (M - 45)) // 60         # 분에서 45를 뺐을 때 시간이 줄어드는지 확인 (예: M이 30이면 H도 줄여야 하니까)
M = (M - 45)                      # 분에서 45분을 빼서 알람 시간을 계산

if M < 0:                         # 만약 분이 음수가 되면 (0보다 작아지면)
    M += 60                       # 분을 60 더해서 시계를 되돌림 (ex: -15 → 45분)

N = H - R                         # R이 1이면 시간을 1시간 줄임

if N < 0:                         # 시간이 0보다 작아지면 (즉, 전날로 넘어가면)
    N += 24                       # 24를 더해서 24시간제 기준으로 처리

print(N, M)                       # 계산된 시와 분을 출력


# 실행은 잘되나 실패라고 떠서 챗GPT한테 물어봤더니 계산이 불안정하다고 함.
아래는 지피티가 수정한 코드 
------------------------------------------------------------------------------------------------------------------------------

# H, M = map(int, input().split())  # 시간(H)과 분(M)을 입력받음
#
# if M < 0:                         # 만약 분이 음수가 되면 (0보다 작아지면)
#     M += 60                       # 분을 60 더해서 시계를 되돌림 (ex: -15 → 45분)
#     H -= 1                        # R이 1이면 시간을 1시간 줄임
#
# if H < 0:                         # 시간이 0보다 작아지면 (즉, 전날로 넘어가면)
#     H += 24                       # 24를 더해서 24시간제 기준으로 처리
#
# print(H, M)                       # 계산된 시와 분을 출력
#
