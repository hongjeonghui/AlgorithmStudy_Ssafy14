import sys
sys.stdin = open('input.txt', 'r')

# input 데이터 리스트(int)로 묶음
arr = [int(input()) for _ in range(9)]
sum_arr = sum(arr)
# 합이 100이 넘는 애들을 타겟으로 그 둘의 합을 구해둠
target = sum_arr - 100

idx1 = -1
idx2 = -1
found = False

#i, j 중복 없이 하나씩 확인하기 위해서 i는 뒤에서부터, j는 i를 범위로 잡고 순회
for i in range(8, -1, -1):
    for j in range(i):
		    # 해당 값들의 합이 위에 구한 target이랑 같다면 인덱스 킵
        if arr[i] + arr[j] == target:
            idx1, idx2 = i, j
            found = True
            break
    if found:
        break

# 위에서 구한 인덱스의 값들을 제외한 나머지 값을 새로운 리스트에 추가
result = []
# 인덱스와 값을 enumerate를 사용해서 배열 안에서 각각 뽑아내기 
for idx, value in enumerate(arr):
		# 타겟 인덱스에 arr 요소들의 인덱스가 같지 않으면 새로운 인덱스에 추가
    if idx not in (idx1, idx2):
        result.append(value)

# 오름차순으로 정리 
result.sort()
for num in result:
    print(num)
